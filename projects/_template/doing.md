# <slug> — doing

<!--
Free-form expansions for items currently under `## Doing` in the overview.

Per rules/TEMPLATE.md:
- One block per expanded item: `## [<id>] <title>` (title byte-equal to overview).
- Blocks ordered top-to-bottom to mirror the overview (newest first).
- Expansions migrate here from the source section file on `pick`.
-->
